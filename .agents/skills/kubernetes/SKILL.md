---
name: kubernetes
description: Interact with Kubernetes clusters. Use whenever the user asks about Kubernetes clusters, contexts, namespaces, pods, or other cluster resources.
---

# Kubernetes

- Always use the `kubernetes-*` MCP server tools first for any cluster interaction.
- Only fall back to raw `kubectl` for genuine edge cases the MCP tools can't handle.
